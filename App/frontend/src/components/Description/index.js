import {DescriptionContainer, DescriptionText} from "./Elements";

const Description = () => {
    return (
        <>
            <DescriptionContainer>
                <DescriptionText >This is a demo of the Rahnema College ML internship final project. You can upload a log file which contains
                    the server logs in order to detect the crawlers of your own website</DescriptionText>
            </DescriptionContainer>
        </>
    );
};

export default Description;